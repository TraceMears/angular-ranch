import { NumberValueAccessor } from '@angular/forms';

export class Employee {
    emp_id: number;
    id: number;
    emp_name: string;
    name: string;
    emp_role: string;
    emp_salary: number;
}
